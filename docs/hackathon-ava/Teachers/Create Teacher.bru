meta {
  name: Create Teacher
  type: http
  seq: 1
}

post {
  url: {{base_url}}/teachers/create
  body: json
  auth: inherit
}

body:json {
  {
    "name": "John Doe",
    "email": "john.doe@example.com"
  }
}
